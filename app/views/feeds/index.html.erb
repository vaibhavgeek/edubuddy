<%= params.inspect %>	
<%= form_for :feed , url: channel_feeds_path(:channel_id =>1) do |f| %>
<%= f.text_area :content ,  class: 'form-control'%>
<%= f.submit "Post" , class: "btn btn-primary pull-right" %>
<% end %>

<% @showfeeds.each do |feed| %>
	<%= feed.content %>
	<% end %>